/**
 * moving-within-europe-page controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::moving-within-europe-page.moving-within-europe-page');
